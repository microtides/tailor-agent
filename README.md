# tailor-agent

Binaries of the tailor agent, one directory per version under `agent/`. A version, once published, never changes. Written by the release workflow of `microtides/tailor`; nothing else is in here.
